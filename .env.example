# Server
PORT=5000
NODE_ENV=development
ALLOWED_ORIGIN=http://localhost:3000

# Database (Prisma MySQL)
DATABASE_URL="mysql://root:@localhost:3306/client_management"

# Optional separate DB variables (if you prefer to use them in your code)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=client_management
DB_PORT=3306
